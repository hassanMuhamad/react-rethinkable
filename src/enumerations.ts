export enum SUPPORTED_CONTENT_TYPES {
    JSON = "json",
    RAW = "text",
    DEFAULT = "blob",
}

export enum SPEC_HEADER_KEYS {
    CONTENT_TYPE = "content-type",
}

export enum FALLBACK_VALUES {
    CONTENT_TYPE = "default",
}
